import styled from 'styled-components'

const Nav = styled.nav`
    background-color: black;
    height: 80px;
    color: whitesmoke;
`

const Navbar = () => {
    return (
        <Nav>
            <div>Menu</div>
        </Nav>
    )
}

export default Navbar
